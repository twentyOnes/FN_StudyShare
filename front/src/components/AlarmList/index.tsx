import React from 'react';
import styled from '@emotion/styled';

const AlarmBox = styled.button``;
const Icon = styled.div``;

const AlarmList = () => {
  return (
    <div>
      <AlarmBox>
        <Icon></Icon>
      </AlarmBox>
    </div>
  );
};

export default AlarmList;
